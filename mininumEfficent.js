// odd number, delete one, pair the remained number,find the mininum differ
// for example: [1,2,4,6,7] return2. remove 4, (2-1)+(7-6)=2, numbers in the array can repeat.

const miniEfficent = (array) => {};
